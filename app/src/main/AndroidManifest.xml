<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.programminginmyway.todoappnew">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.SplashThemeNew">
<!--    testing compose screen-->
<!--        <activity-->
<!--            android:name=".compose.ComposeDemo"-->
<!--            android:exported="true"/>-->
<!--            android:theme="@style/Theme.MyApp">-->
        <activity
            android:name=".screens.RegistrationScreen"
            android:exported="false"
            android:label="@string/registration_title"
            android:launchMode="singleTop"
            android:parentActivityName=".screens.LoginScreen" />
        <activity
            android:name=".screens.ForgotPasswordScreen"
            android:exported="false"
            android:label="@string/forgotten_password"
            android:parentActivityName=".screens.LoginScreen" />
        <activity
            android:name=".screens.MainScreen"
            android:exported="false"
            android:label="@string/task_list" />
        <activity
            android:name=".screens.LoginScreen"
            android:exported="false"
            android:label="@string/login_title" />
        <activity
            android:name=".screens.ChangePassword"
            android:exported="false"
            android:label="@string/change_password"
            android:parentActivityName=".screens.MainScreen" />
        <activity
            android:name=".screens.SplashScreenActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:theme="@style/Theme.SplashThemeNew">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>